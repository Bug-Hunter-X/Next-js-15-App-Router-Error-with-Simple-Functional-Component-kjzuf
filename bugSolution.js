```javascript
// No changes needed in the component itself. Ensure your Next.js app is properly set up with correct dependencies and configurations. 
// Re-running npm install and verifying Next.js version might resolve the issue.
// pages/index.js

export default function Home() {
  return (
    <div>Hello world</div>
  );
}
```